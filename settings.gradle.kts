pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id("org.jetbrains.intellij.platform") version "2.16.0"
    }
}

rootProject.name = "gitredminejump"
